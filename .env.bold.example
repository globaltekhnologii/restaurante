# ⚠️ INSTRUCCIONES DE SEGURIDAD - LEER PRIMERO

## Configuración de Credenciales Bold

Este archivo es una PLANTILLA. NO contiene credenciales reales.

### Pasos para configurar:

1. Copia este archivo como `.env` en la raíz del proyecto
2. Reemplaza los valores de ejemplo con tus credenciales reales
3. NUNCA subas el archivo `.env` a GitHub (ya está en .gitignore)

---

## Credenciales Bold - PRODUCCIÓN

# Llave de Identidad (Pública - se usa en frontend)
BOLD_PUBLIC_KEY=teNcT5WmC-ax0ihGzVSL4BLO3z134DMPmwGY_ufODzk

# Llave Secreta (PRIVADA - NUNCA expongas en frontend)
BOLD_SECRET_KEY=TU_LLAVE_SECRETA_AQUI

# Merchant ID (si lo tienes)
BOLD_MERCHANT_ID=

# Modo (production o sandbox)
BOLD_MODE=production

# URLs de Bold API
BOLD_API_URL=https://api.bold.co/v1
BOLD_CHECKOUT_URL=https://checkout.bold.co

# URL de retorno después del pago
BOLD_RETURN_URL=http://localhost/Restaurante/pago_confirmacion.php

# URL del webhook
BOLD_WEBHOOK_URL=http://localhost/Restaurante/api/webhook_bold.php

---

## ⚠️ IMPORTANTE

- La LLAVE SECRETA debe permanecer en el servidor
- NUNCA la incluyas en JavaScript del frontend
- Usa HTTPS en producción
- Cambia las URLs localhost por tu dominio real en producción
